<div id="header">
	<div id="title">HEADER</div>
	<div id="fullsite">
			<% if user_signed_in? %>
			<%= link_to 'Logout', logout_path %>
			<% else %>
			<%= link_to 'Login', login_path%>
			<% end %>
	</div>
</div>

<%= %>
